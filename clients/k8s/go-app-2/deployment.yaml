apiVersion: v1
kind: Namespace
metadata:
  name: user-cluster-2
---
apiVersion: v1
kind: Secret
metadata:
  name: user-db-secret
  namespace: user-cluster-2
type: Opaque
stringData:
  postgres-password: postgres123
  postgres-user: postgres
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: user-app-config
  namespace: user-cluster-2
data:
  DB_HOST: user-postgres
  DB_PORT: "5432"
  DB_NAME: user_db
  DB_USER: postgres
  PORT: "8080"
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: user-postgres
  namespace: user-cluster-2
spec:
  serviceName: user-postgres
  replicas: 1
  selector:
    matchLabels:
      app: user-postgres
  template:
    metadata:
      labels:
        app: user-postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        env:
        - name: POSTGRES_DB
          value: user_db
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: user-db-secret
              key: postgres-user
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: user-db-secret
              key: postgres-password
        ports:
        - containerPort: 5432
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
  volumeClaimTemplates:
  - metadata:
      name: postgres-data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: user-postgres
  namespace: user-cluster-2
spec:
  selector:
    app: user-postgres
  ports:
  - port: 5432
    targetPort: 5432
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: user-cluster-2
  labels:
    app: user-service
    database: user_db
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
        database: user_db
      annotations:
        database.host: user-postgres
        database.port: "5432"
        database.name: user_db
        database.user: postgres
    spec:
      containers:
      - name: app
        image: go-app-2:latest
        envFrom:
        - configMapRef:
            name: user-app-config
        env:
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: user-db-secret
              key: postgres-password
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: user-cluster-2
spec:
  selector:
    app: user-service
  ports:
  - port: 8080
    targetPort: 8080

