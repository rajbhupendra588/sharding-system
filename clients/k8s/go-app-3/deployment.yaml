apiVersion: v1
kind: Namespace
metadata:
  name: order-cluster-3
---
apiVersion: v1
kind: Secret
metadata:
  name: order-db-secret
  namespace: order-cluster-3
type: Opaque
stringData:
  postgres-password: postgres123
  postgres-user: postgres
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: order-app-config
  namespace: order-cluster-3
data:
  DB_HOST: order-postgres
  DB_PORT: "5432"
  DB_NAME: order_db
  DB_USER: postgres
  PORT: "8080"
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: order-postgres
  namespace: order-cluster-3
spec:
  serviceName: order-postgres
  replicas: 1
  selector:
    matchLabels:
      app: order-postgres
  template:
    metadata:
      labels:
        app: order-postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        env:
        - name: POSTGRES_DB
          value: order_db
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: order-db-secret
              key: postgres-user
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: order-db-secret
              key: postgres-password
        ports:
        - containerPort: 5432
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
  volumeClaimTemplates:
  - metadata:
      name: postgres-data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: order-postgres
  namespace: order-cluster-3
spec:
  selector:
    app: order-postgres
  ports:
  - port: 5432
    targetPort: 5432
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: order-cluster-3
  labels:
    app: order-service
    database: order_db
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
        database: order_db
      annotations:
        database.host: order-postgres
        database.port: "5432"
        database.name: order_db
        database.user: postgres
    spec:
      containers:
      - name: app
        image: go-app-3:latest
        envFrom:
        - configMapRef:
            name: order-app-config
        env:
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: order-db-secret
              key: postgres-password
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: order-cluster-3
spec:
  selector:
    app: order-service
  ports:
  - port: 8080
    targetPort: 8080

