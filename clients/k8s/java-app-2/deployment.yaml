apiVersion: v1
kind: Namespace
metadata:
  name: inventory-cluster-5
---
apiVersion: v1
kind: Secret
metadata:
  name: inventory-db-secret
  namespace: inventory-cluster-5
type: Opaque
stringData:
  postgres-password: postgres123
  postgres-user: postgres
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: inventory-app-config
  namespace: inventory-cluster-5
data:
  DB_HOST: inventory-postgres
  DB_PORT: "5432"
  DB_NAME: inventory_db
  DB_USER: postgres
  PORT: "8080"
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: inventory-postgres
  namespace: inventory-cluster-5
spec:
  serviceName: inventory-postgres
  replicas: 1
  selector:
    matchLabels:
      app: inventory-postgres
  template:
    metadata:
      labels:
        app: inventory-postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        env:
        - name: POSTGRES_DB
          value: inventory_db
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: inventory-db-secret
              key: postgres-user
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: inventory-db-secret
              key: postgres-password
        ports:
        - containerPort: 5432
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
  volumeClaimTemplates:
  - metadata:
      name: postgres-data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-postgres
  namespace: inventory-cluster-5
spec:
  selector:
    app: inventory-postgres
  ports:
  - port: 5432
    targetPort: 5432
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: inventory-cluster-5
  labels:
    app: inventory-service
    database: inventory_db
spec:
  replicas: 2
  selector:
    matchLabels:
      app: inventory-service
  template:
    metadata:
      labels:
        app: inventory-service
        database: inventory_db
      annotations:
        database.host: inventory-postgres
        database.port: "5432"
        database.name: inventory_db
        database.user: postgres
    spec:
      containers:
      - name: app
        image: java-app-2:latest
        envFrom:
        - configMapRef:
            name: inventory-app-config
        env:
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: inventory-db-secret
              key: postgres-password
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "400m"
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: inventory-cluster-5
spec:
  selector:
    app: inventory-service
  ports:
  - port: 8080
    targetPort: 8080

