apiVersion: v1
kind: Namespace
metadata:
  name: ecommerce-ns
---
apiVersion: v1
kind: Secret
metadata:
  name: ecommerce-db-secret
  namespace: ecommerce-ns
type: Opaque
stringData:
  postgres-password: postgres123
  postgres-user: postgres
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: ecommerce-db-config
  namespace: ecommerce-ns
data:
  POSTGRES_DB: ecommerce_db
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ecommerce-postgres
  namespace: ecommerce-ns
spec:
  serviceName: ecommerce-postgres
  replicas: 1
  selector:
    matchLabels:
      app: ecommerce-postgres
  template:
    metadata:
      labels:
        app: ecommerce-postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: ecommerce-db-secret
              key: postgres-user
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: ecommerce-db-secret
              key: postgres-password
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              name: ecommerce-db-config
              key: POSTGRES_DB
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
  - metadata:
      name: postgres-data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: ecommerce-postgres
  namespace: ecommerce-ns
spec:
  selector:
    app: ecommerce-postgres
  ports:
  - port: 5432
    targetPort: 5432
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecommerce-app
  namespace: ecommerce-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ecommerce-app
  template:
    metadata:
      labels:
        app: ecommerce-app
    spec:
      containers:
      - name: app
        image: ecommerce-app:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        env:
        - name: PORT
          value: "8080"
        - name: DB_HOST
          value: ecommerce-postgres.ecommerce-ns.svc.cluster.local
        - name: DB_PORT
          value: "5432"
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: ecommerce-db-secret
              key: postgres-user
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: ecommerce-db-secret
              key: postgres-password
        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: ecommerce-db-config
              key: POSTGRES_DB
        - name: DATABASE_URL
          value: postgres://postgres:postgres123@ecommerce-postgres.ecommerce-ns.svc.cluster.local:5432/ecommerce_db
        - name: DATABASE_HOST
          value: ecommerce-postgres.ecommerce-ns.svc.cluster.local
        - name: DATABASE_PORT
          value: "5432"
        - name: DATABASE_NAME
          value: ecommerce_db
        - name: DATABASE_USER
          value: postgres
---
apiVersion: v1
kind: Service
metadata:
  name: ecommerce-app
  namespace: ecommerce-ns
spec:
  selector:
    app: ecommerce-app
  ports:
  - port: 8080
    targetPort: 8080

