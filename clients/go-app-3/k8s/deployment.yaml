apiVersion: v1
kind: Namespace
metadata:
  name: orders-ns
---
apiVersion: v1
kind: Secret
metadata:
  name: orders-db-secret
  namespace: orders-ns
type: Opaque
stringData:
  postgres-password: postgres123
  postgres-user: postgres
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: orders-db-config
  namespace: orders-ns
data:
  POSTGRES_DB: orders_db
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: orders-postgres
  namespace: orders-ns
spec:
  serviceName: orders-postgres
  replicas: 1
  selector:
    matchLabels:
      app: orders-postgres
  template:
    metadata:
      labels:
        app: orders-postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: orders-db-secret
              key: postgres-user
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: orders-db-secret
              key: postgres-password
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              name: orders-db-config
              key: POSTGRES_DB
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
  - metadata:
      name: postgres-data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: orders-postgres
  namespace: orders-ns
spec:
  selector:
    app: orders-postgres
  ports:
  - port: 5432
    targetPort: 5432
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders-app
  namespace: orders-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: orders-app
  template:
    metadata:
      labels:
        app: orders-app
    spec:
      containers:
      - name: app
        image: orders-app:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        env:
        - name: PORT
          value: "8080"
        - name: DB_HOST
          value: orders-postgres.orders-ns.svc.cluster.local
        - name: DB_PORT
          value: "5432"
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: orders-db-secret
              key: postgres-user
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: orders-db-secret
              key: postgres-password
        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: orders-db-config
              key: POSTGRES_DB
        - name: DATABASE_URL
          value: postgres://postgres:postgres123@orders-postgres.orders-ns.svc.cluster.local:5432/orders_db
        - name: DATABASE_HOST
          value: orders-postgres.orders-ns.svc.cluster.local
        - name: DATABASE_PORT
          value: "5432"
        - name: DATABASE_NAME
          value: orders_db
        - name: DATABASE_USER
          value: postgres
---
apiVersion: v1
kind: Service
metadata:
  name: orders-app
  namespace: orders-ns
spec:
  selector:
    app: orders-app
  ports:
  - port: 8080
    targetPort: 8080

